---
title: "Quickstart"
description: "Make your first API call in under 5 minutes"
icon: "bolt"
---

This guide walks you through making your first Partna API call. By the end, you will have fetched live conversion rates and understand the basic request pattern for every endpoint.

## 1. Get your API credentials

<Steps>
  <Step title="Request access">
    Fill out the [onboarding form](https://forms.getpartna.com/get-started). The Partna team will reach out to schedule a brief onboarding call and set up your merchant account.
  </Step>
  <Step title="Create your API keys">
    Once your merchant account is set up, log in to the Partna dashboard and generate your API keys. You will get separate keys for **staging** and **production**:

    - Staging dashboard: [staging-dashboard.getpartna.com](https://staging-dashboard.getpartna.com/)
    - Production dashboard: [dashboard.getpartna.com](https://dashboard.getpartna.com/)

    Each request requires two credentials:

    - `x-api-user`: Your merchant username
    - `x-api-key`: Your API key (generated from the dashboard)
  </Step>
  <Step title="Use staging first">
    Build and test against the staging environment before deploying to production. Staging supports mock deposits and test transactions at no cost.
  </Step>
</Steps>

## 2. Make your first request

Fetch the current conversion rates. This is the simplest authenticated request and confirms your credentials work.

<CodeGroup>

```bash cURL
curl --request GET \
  --url https://staging-api.getpartna.com/v4/rate \
  --header 'x-api-key: YOUR_API_KEY' \
  --header 'x-api-user: YOUR_USERNAME'
```

```python Python
import requests

response = requests.get(
    "https://staging-api.getpartna.com/v4/rate",
    headers={
        "x-api-key": "YOUR_API_KEY",
        "x-api-user": "YOUR_USERNAME"
    }
)

print(response.json())
```

```javascript Node.js
const response = await fetch("https://staging-api.getpartna.com/v4/rate", {
  headers: {
    "x-api-key": "YOUR_API_KEY",
    "x-api-user": "YOUR_USERNAME"
  }
});

const data = await response.json();
console.log(data);
```

</CodeGroup>

## 3. Check the response

A successful response returns current conversion rates:

```json
{
  "data": {
    "rate": {
      "USDT_to_USD": {
        "fromAmount": 1,
        "fromCurrency": "USDT",
        "key": "1-USDT_to_USD-bbfcdc61-1755873777",
        "rate": 1,
        "toAmount": 1,
        "toCurrency": "USD"
      },
      "ETH_to_USD": {
        "fromAmount": 1,
        "fromCurrency": "ETH",
        "key": "2400-ETH_to_USD-5ddc8f46-1755873777",
        "rate": 2400,
        "toAmount": 2400,
        "toCurrency": "USD"
      }
    }
  },
  "message": "success"
}
```

Each rate object includes a `key` field. You will use this key when initiating ramp transactions to lock in the quoted rate.

## 4. Set up your webhook endpoint

Before processing real transactions, configure a webhook URL to receive status updates. You can do this via the [dashboard](https://dashboard.getpartna.com/) or the [Update Webhook URL](/api-reference/user/update-webhook-url) endpoint.

Your webhook endpoint should return a `200` status code immediately upon receiving the event. Process the event asynchronously.

## Next steps

You are now authenticated and can fetch rates. Here is what to build next, depending on your use case:

| Use case | Guide |
| --- | --- |
| Users convert local currency to crypto | [Onramp Guide](/guides/onramp) |
| Users convert crypto to local currency | [Offramp Guide](/guides/offramp) |
| Collect payments and settle in USD/stablecoins | [Collect and Settle Guide](/guides/collect-and-settle) |
| Verify user identity for transactions | [Accounts and KYC Guide](/guides/accounts-and-kyc) |
| Test with mock transactions in staging | [Testing Guide](/testing) |